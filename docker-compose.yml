version: '3.8'

services:
  node-mailer-app:
    build: .
    command: npm run dev
    ports:
      - "3050:3000"
    env_file:
      - ./.env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules 
    